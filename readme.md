# bashrc

my bash profile


# features

 - git branch
 - some useful alias
 - auto venv active

# installation

    $ sh install.sh

makes `bashrc`, `bash_alias`, `bash_env` symbolic link. file exists in `$HOME`, move to `$HOME/bashrc.old`.
